export const DEBUG = `${process.env.DEBUG}` as "true" | "false";
export const API_CLIENT = `${process.env.API_CLIENT}` as "axios";
export const ADMIN_USERNAME = `${process.env.ADMIN_USERNAME}`;
export const ADMIN_PASSWORD = `${process.env.ADMIN_PASSWORD}`;
export const TESTS = `${process.env.TESTS}` as "ui" | "api";
export const HEADLESS = `${process.env.HEADLESS}` as "true" | "false";
